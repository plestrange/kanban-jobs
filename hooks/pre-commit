#!/usr/bin/env bash
# Delegates to Makefile targets — see docs/ARCHITECTURE.md §9 and CONTRIBUTING.md.
# check-staged first: the data/ guard is the one check that must never be skipped.
set -euo pipefail
make check-staged
make lint
make typecheck
make test
